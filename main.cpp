// main.cpp
#include <stdio.h>
// for using hearder, you have to include headers
// and if you make main function you don't need to return 0
#include "sum.h"

int main() {
	int s = sum(10);
	printf("result=%d\n", s);
}

